---
status: retired
retired: "2026-06"
canonical: claude.md
---

# 已退役：`claude-desktop`

> **2026-06**：**`claude-desktop`** 已从闭集宿主移除，不再出现在 `RUNTIME_REGISTRY.json` → `host_targets.supported`。

Claude 集成请使用 **`claude-code`**（终端 hooks + MCP 可选），手册见 **[`claude.md`](claude.md)**。

历史 Desktop / Cowork / MCP 运维笔记已归档；勿再执行 `install --to claude-desktop` 或依赖 `docs/hosts/claude-desktop-networking.md`。
