interface:
  display_name: "Excel XLSX"
  short_description: "Read, edit, and review .xlsx workbooks"
  default_prompt: "Use $xlsx to inspect, create, edit, or audit this `.xlsx` workbook while preserving formulas, styles, sheet structure, and layout-sensitive elements."
