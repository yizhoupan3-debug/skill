---
name: legacy-gsd-ci-stub
description: CI-only placeholder for retired /gsd-* framework_commands.gsd (surface_publish false). Not a daily entrypoint.
routing_layer: L0
routing_owner: legacy-gsd
routing_gate: none
user-invocable: false
metadata:
  version: "0.0.1"
  platforms: [supported]
  tags: [legacy, ci-stub, retired]
---

# legacy-gsd-ci-stub

Retired GSD lifecycle. Personal default: `/discussx` → `/planx` → `/implementx` → `/verifyx`.

`framework_goal_drive` example (pre-execution): `"drive_until_done":false`, `"status":"planned"`.
