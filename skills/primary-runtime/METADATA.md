# primary-runtime

非 skill 目录。存放 primary-runtime 相关 skill 的宿主结构与共享参考资料。

当前内容：
- `spreadsheets/` — 已注册 skill（slug: `spreadsheets`），含 SKILL.md、agents、references、templates。
- `references/artifact-protocol.md` — artifact gate 共享协议（`$doc`、`$pdf`、`$spreadsheets` 等共用）。

本目录不在 SKILL_MANIFEST.json 中注册；子目录 `spreadsheets` 已独立注册为 skill。
