interface:
  display_name: "Paper Reviewer"
  short_description: "Judge whether a paper is ready to submit"
  default_prompt: "Use $paper-reviewer as the review specialist lane behind $paper-workbench. Prefer it for explicit review-only requests or single-dimension review slices. Review and decide; do not drift into direct rewriting."
