# Codex network / local endpoint notes

The system copy uses the same loopback VibeProxy endpoint as the local `imagegen` skill:

- `http://127.0.0.1:8318/v1/responses`

This path is used because the skill standardizes on one direct local Responses route.
