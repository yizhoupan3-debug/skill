[project]
name = "skill-framework"
version = "0.0.0"
description = "Minimal Python surface for skill-framework hook tests and CI (uv-only)."
requires-python = ">=3.12,<3.13"
dependencies = ["requests>=2.28.0", "toml>=0.10.2"]

[tool.uv]
package = false
