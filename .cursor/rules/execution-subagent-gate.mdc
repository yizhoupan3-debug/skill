---
description: Require intelligent execution lane split for implementation tasks
alwaysApply: true
---

# Execution Subagent Gate（仅 Cursor）

跨宿主基线与叙事见 `AGENTS.md` → **Execution Ladder** → **宿主优先级**。**本文件与 `AGENTS.md` 一起构成 Cursor 执行面真源**：凡可分解为多 lane 的实现类工作，默认使用独立 subagent lanes；主线程保留架构决策、集成与最终验证。

细则（触发画像、lane 契约、拒因集合、禁止向用户暴露拒因元说明、`/autopilot` / `$autopilot` goal-style 连续性、closeout）：一律以 `AGENTS.md` 同章及 **Autopilot Goal Mechanism**、**Closeout** 为准。

**显式豁免**：用户要求不使用 subagent（例如 "do not use subagent" / 「不要用子代理」）→ 不适用上述默认。
