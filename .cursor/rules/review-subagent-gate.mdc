---
description: Require subagent lanes by default, with explicit no-subagent reasons
alwaysApply: true
---

# Review Subagent Gate（仅 Cursor）

与 `AGENTS.md` **Execution Ladder** → **宿主优先级**配对。**本文件与 `AGENTS.md` 一起构成 Cursor 执行面真源**：评审、委托、深度/跨模块 review、PR 审查、以及显式编排入口（如 `/autopilot`、`$autopilot`、`/team`、`$team`、`/gitx`、`$gitx`）等场景，默认应拆独立 reviewer / worker lanes；主线程负责集成与最终验证。

细则（何时必须开 lane、拒因集合、禁止向用户暴露拒因元说明、goal-style 收口要求）：以 `AGENTS.md` **Execution Ladder** 与 **Autopilot Goal Mechanism** 为准。

**显式豁免**：用户要求避免 subagent（例如 "do not use subagent" / 「不要用子代理」）→ 本 gate 不适用。
