---
description: Cursor-only hard review gate delta
alwaysApply: true
---

# Review Subagent Gate（仅 Cursor）

跨宿主 review 叙事、拒因集合与收口原则见 `AGENTS.md`。本文件只保留 Cursor 独有硬约束：

- 深度 / 跨模块 / PR 级 review 在 Cursor 上由 `router-rs` hook 维护独立上下文证据链。
- 宿主硬门控以 `.cursor/hook-state` 和 Stop 上的 `router-rs REVIEW_GATE` 单行短码为真源。
- review lane 必须只读：输出 findings、风险、缺测与证据锚点；不改代码、不顺手修复。
- 用户明确要求避免 subagent 时，本 gate 不适用。
