# /planx

Personal lifecycle — plan/roadmap (doc-only).

Read and follow `skills/planx/SKILL.md`. Write under `artifacts/current/<task_id>/` only; declare serial/parallel lanes in `WAVE_STATE.json`. Do not change product code.
