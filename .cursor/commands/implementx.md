# /implementx

Personal lifecycle — execute all waves in one breath.

Read and follow `skills/implementx/SKILL.md`. Drive via `framework_goal_drive` stdio + `artifacts/current/<task_id>/` (`WAVE_STATE.json`, `GOAL_STATE.json`). Execute every wave without pausing at wave boundaries for user confirmation. Spawn parallel lanes; merge via `lane-notes/*.md` only.
