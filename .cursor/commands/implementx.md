# /implementx

Personal lifecycle — execute all waves in one breath.

Read and follow `skills/implementx/SKILL.md`. Execute every wave in `WAVE_STATE.json` without pausing at wave boundaries for user confirmation. Spawn parallel lanes; merge via `lane-notes/*.md` only.
