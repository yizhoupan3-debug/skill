# /update

Refresh key docs, git tracking, and stale/dead repo surfaces; run framework generated-artifact checks when applicable.

Read and follow `skills/update/SKILL.md`. Use only when the user explicitly invokes `/update`.
