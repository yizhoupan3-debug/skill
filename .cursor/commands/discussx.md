# /discussx

Personal lifecycle — discuss/requirements (doc-only). **Stay in discuss** until you explicitly say `/planx` or plan intent; each turn may add angles and constraints.

Read and follow `skills/discussx/SKILL.md`. Write under `artifacts/current/<task_id>/` only; do not change product code.
