---
description: Cross-host orchestration gate when user mentions workflow/team/ultracode (agent-swarm gate)
alwaysApply: true
---

# Orchestration Gate（Cross-host）

跨宿主基线见 [AGENTS.md](../AGENTS.md) · [docs/architecture.md](../docs/architecture.md)；编排细则见 [agent-swarm-orchestration/SKILL.md](../skills/agent-swarm-orchestration/SKILL.md)。

## 何时生效（当前用户消息）

当用户消息含 **编排意图**（如 `team`、`多 agent 团队`、`agent 编排`、`workflow`、`ultracode`），且**不是**仅指 GitHub Actions / CI workflow 失败时：

## 硬约束

1. **立即 Read** `skills/agent-swarm-orchestration/SKILL.md`（Orchestration mode / Team triggers / Main-thread contract 三节）。
2. 首行输出机读：`orchestration: { mode, trigger, reason }`。
3. 任何宿主上 `mode` 可选 **`team`**（`session-supervisor` team API）或 `sidecar`（bounded subagent）。**Workflow（JS 编排）已彻底移除**，不要再引用 `.claude/workflows/` 或 `workflow-supervisor-protocol`。
4. Team 模式下：通过 `artifacts/teams/` 文件系统管理团队；agent 间通信通过 `team_send_message` / `team_read_messages`。
5. 所有 agent 完成时必须 `team_remove_member` 或 `agent_unregister`，确保资源释放。

## 非编排轮次

未满足上文「何时生效」时，本节不追加行为。
