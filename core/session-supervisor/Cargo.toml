[package]
name = "session-supervisor"
version = "0.1.0"
edition = "2024"
description = "L6 multi-agent supervisor with Quality Gate closeout loop"
[lints]
workspace = true

[lib]
name = "session_supervisor"
path = "src/lib.rs"

[dependencies]
core-errors = { path = "../core-errors" }
framework-core = { path = "../framework-core" }
framework-runtime = { path = "../framework-runtime" }
core-state-utils = { path = "../core-state-utils" }
core-state = { path = "../core-state" }
observability-core = { path = "../observability-core" }
runtime-storage = { path = "../runtime-storage" }
chrono.workspace = true
libc.workspace = true
regex.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true

[dev-dependencies]
tempfile.workspace = true
