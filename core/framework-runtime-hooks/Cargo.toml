[package]
name = "framework-runtime-hooks"
version = "0.1.0"
edition = "2024"
description = "L0 fn-pointer registry (OnceLock) — cross-layer communication, zero business logic"

[lints]
workspace = true

[lib]
name = "framework_runtime_hooks"

[dependencies]
core-errors = { path = "../core-errors" }
serde_json.workspace = true
