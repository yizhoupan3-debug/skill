[package]
name = "browser-mcp-dispatch"
version = "0.1.0"
edition = "2024"
description = "L0 browser MCP dispatch helper — stdin/stdout MCP relay with host isolation"
[lints]
workspace = true

[lib]
name = "browser_mcp_dispatch"
path = "src/lib.rs"

[dependencies]
core-errors = { path = "../core-errors" }
framework-kernel = { path = "../framework-kernel" }
serde_json.workspace = true
tracing.workspace = true
