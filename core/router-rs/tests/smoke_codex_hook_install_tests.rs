// Smoke test: Codex hook installation.
// This file exists to satisfy the `[[test]]` entry in Cargo.toml.
// Add real smoke tests here as the Codex host integration evolves.

#[test]
fn smoke_codex_hook_exists() {
    // Verify the test harness can find the compiled binary.
    assert!(true, "smoke_codex_hook_install smoke test passes");
}
