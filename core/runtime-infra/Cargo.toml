[package]
name = "runtime-infra"
version = "0.1.0"
edition = "2024"
description = "L5 runtime infrastructure: env/path/time/io/sync/compute/bootstrap"
[lints]
workspace = true

[dependencies]
# B0
core-state = { path = "../core-state" }
core-state-utils = { path = "../core-state-utils" }
core-errors = { path = "../core-errors" }
framework-core = { path = "../framework-core" }
framework-runtime = { path = "../framework-runtime" }

# L0/L1
host-projection = { path = "../host-projection" }
routing-engine = { path = "../routing-engine" }

# L4
runtime-storage = { path = "../runtime-storage" }

# workspace
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
which.workspace = true
