[package]
name = "tool-routing-engine"
version = "0.1.0"
edition = "2024"
description = "Tool routing engine: scoring pipeline, routing, and search for MCP tools."
[lints]
workspace = true

[dependencies]
core-state-utils = { path = "../core-state-utils" }
routing-core = { path = "../routing-core" }
mcp-tool-registry = { path = "../mcp-tool-registry" }
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true

[dev-dependencies]
criterion = { workspace = true }
insta.workspace = true
tempfile.workspace = true

[[bench]]
name = "routing_bench"
harness = false
