[package]
name = "routing-engine"
version = "0.1.0"
edition = "2024"
description = "Skill routing engine: 16-step scoring pipeline, routing, search, and evaluation for skill routing."
[lints]
workspace = true

[dependencies]
core-state-utils = { path = "../core-state-utils" }
routing-core = { path = "../routing-core" }
notify.workspace = true
rayon.workspace = true
regex.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true
skill-layer = { path = "../skill-layer" }

[dev-dependencies]
criterion = { workspace = true }
insta.workspace = true
tempfile.workspace = true

[[bench]]
name = "scoring_bench"
harness = false
