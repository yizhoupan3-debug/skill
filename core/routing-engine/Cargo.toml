[package]
name = "routing-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
notify = "7.0"
regex = "1.11.1"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
tokio = { version = "1.44", features = ["sync"] }

[dev-dependencies]
tempfile = "3.15"
