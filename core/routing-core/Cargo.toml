[package]
name = "routing-core"
version = "0.1.0"
edition = "2024"
description = "Shared routing primitives: fuzzy matching (trigram Jaccard) for skill and tool routing."
[lints]
workspace = true

[dependencies]
core-state-utils = { path = "../core-state-utils" }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "fuzzy_bench"
harness = false
