# /verifyx

Personal lifecycle — verify + ship in one command.

Read and follow `skills/verifyx/SKILL.md`. Run validation commands, update `EVIDENCE_INDEX.json`, write closeout, complete `GOAL_STATE.json`. After closeout, purge `artifacts/current/<task_id>/` (see verifyx § Post-verify task-dir purge).
