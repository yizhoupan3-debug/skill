# /gitx

Git closeout workflow — review substantive diff, fix, tidy, commit, branch, merge, push.

Read and follow `skills/gitx/SKILL.md`. Use only when the user explicitly invokes `/gitx` or `gitx`.
