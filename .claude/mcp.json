{
  "mcpServers": {
    "router-rs-framework": {
      "args": [
        "claude-desktop",
        "agent",
        "--repo-root",
        "${workspaceRoot}"
      ],
      "command": "router-rs",
      "description": "Framework runtime snapshot, continuity, skill routing, closeout advisory",
      "type": "stdio"
    }
  }
}
