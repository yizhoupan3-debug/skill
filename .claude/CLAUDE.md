<!-- managed_by: skill-framework · claude-code · keep ≤15 lines -->

# Claude Code（本项目）

Hook 集成：七事件全注册（`.claude/settings.json` + `router-rs claude hook`）。
Goal/RFV via `framework_goal_drive` / `framework_rfv_loop` stdio + `artifacts/current/<task_id>/`。
检查点：`session_checkpoint`（非自动）。

路由：`skills/SKILL_ROUTING_RUNTIME.json` · 产物：`artifacts/current/`。
