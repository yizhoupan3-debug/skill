{
  "mcpServers": {
    "browser-mcp": {
      "args": [
        "browser",
        "mcp-stdio",
        "--repo-root",
        "${REPO_ROOT}"
      ],
      "command": "${ROUTER_RS_BIN}",
      "description": "Browser MCP for external web research (browser_open, browser_get_text, …)",
      "env": {
        "SKILL_FRAMEWORK_ROOT": "${REPO_ROOT}"
      },
      "type": "stdio"
    }
  }
}
